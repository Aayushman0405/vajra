apiVersion: v1
kind: Service
metadata:
  name: vajra-control-plane
  namespace: vajra-system
spec:
  selector:
    app: vajra-control-plane
  ports:
    - port: 80
      targetPort: 8000
      name: http
  type: ClusterIP

